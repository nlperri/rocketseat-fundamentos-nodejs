//Buffer => Representação de um espaço na memória do computador, usado especificamente pra transitar dados rapidamente (salvar e ler da memória de maneira performática). Guarda os dados de maneira binária em hexadecimal (baixo nível).

const buf = Butter.from('ok')

console.log(buf)

console.log(buf.toJSON()) // decimal
